import random

random.random() #Math.random();
random.choice([num, num2, num3]) #choice one from gave
random.randint(1, 6) # choice one between 1 - 6
random.sample(iterator, numofreturnedelement) # возвращает итератор, который воспроизводит указанное число элементов без повторений.